import React, { useState } from 'react'
import SearchImages from 'api/index'

const SearchBar = () => {
    return (
        <div>
            <SearchImages />
        </div>
    )
}
export default SearchBar
